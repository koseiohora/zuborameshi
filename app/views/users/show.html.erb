<div class="contents row" >
  <p><%= @nickname %>さんの投稿一覧</p>
  <% @recipes.each do |recipe| %>
    <h2><%= recipe.title %></h2>
    <p><%= recipe.text %></p>
  <% end %>
  <%= paginate(@recipes) %>
</div>